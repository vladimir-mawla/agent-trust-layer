# CURRENT
- active_loop: L1 BUILD (M5 REJECTED by L4 VERIFY — 6 findings fixed in `m5-policy-fixes`, ready for re-VERIFY)
- target: M5
- iteration: 2
- last_gate: `npm run typecheck` clean (tsconfig.json + tsconfig.lib.json); `npm test` 225/225 green (was 196 at the rejected M5 baseline, +29 new, zero regressions); `npm test -- policy` 60/60 green; `npm run build` (`next build --webpack`) succeeds; `git diff main -- lib/identity/ lib/credentials/ lib/trust/ app/` empty (M1/M3/M4/app untouched); `git diff main -- .genesis/DONE.html .genesis/PLAN.md` empty (locked files untouched); `git status --short` clean at rest; full test run completes well under 1s wall time, exit code 0 (no hang). Mutation-testing proofs re-run and confirmed on this iteration: reverting FINDING 1's `decision-guards.ts` shape checks reproduces the exact reported TypeErrors (4→6 failing cases once the history fixtures actually exercised `history-constraints.ts`'s inner loop); mutating `computeFieldBound` to `return candidates[0]` now breaks 9/225 tests (was 1/196 pre-fix), including the strengthened dedicated `type-boundary.test.ts` proof.
- last_action: Fixed all 6 non-cosmetic findings from the L4 VERIFY pass that REJECTED M5. **(1) engine.ts threw bare TypeErrors for malformed `authority`/`history` inputs** — `validatePolicy` defended `policy` thoroughly but `authority`/`history` (typed as trusted `TrustDecision`s) got no runtime shape check beyond literal `null`; ADR 0004 already says "claims arrive as JSON, not TypeScript", so a value crossing a real boundary could crash the engine. Fixed with `decision-guards.ts` (`checkAuthorityInput`/`sanitizeHistoryInput`): anything not a well-formed decision is now treated exactly as absent — fail closed, structured `PolicyDecision`, never a throw. Reverting the guard and re-running the new tests reproduces the exact reported TypeErrors; restoring it, all pass. **(2) the tighten-never-loosen combinator was validated by coincidence** — mutating `computeFieldBound` to `return candidates[0]` broke only 1/196 tests, and NOT the dedicated `type-boundary.test.ts` proof (its fixture happened to have the credential's own scope, the first candidate pushed, already be the tightest value). Added `permitted-scope.test.ts` with fixtures covering each of the three bound sources being tightest in a NON-first position, and strengthened the existing dedicated proof test itself with a genuinely-narrower case; the same mutation now breaks 9/225, including that dedicated test. **(3) ADR 0004 and `permitted-scope.ts` claimed "`Math.min`, exclusively" while the code was a hand-rolled `<`-reduce** with different NaN semantics (silently drops a poisoned candidate unless it's first). Switched to the real, literal `Math.min`; its NaN-poisoning is now the documented, deliberate, fail-closed behaviour — paired with an explicit `Number.isFinite` check in `engine.ts` so a non-finite bound REFUSES rather than being compared with `>` and read as an unconditional permit. **(4) omitting a bounded scope field bypassed every ceiling** — the bound-checking loop iterated `request.scope`'s own keys, so `{ scope: {} }` against a `maxAmount` ceiling never even consulted the credential's own granted scope. Fixed by iterating the UNION of fields named by the credential's scope, the matching policy rule's `maxScope`, and any triggered history rule's `scopeField`; an omitted-but-bounded field is now refused (documented choice: "omitted = unbounded, refuse", not "omitted = zero, permit" — see ADR 0004 and `engine.ts`'s inline comment). **(5) `credential-verification-failed` had zero test coverage** despite being reachable — added a test reaching it through the real `evaluateAuthorityCredentialTrust` with a malformed JWT. **(6) the mandatory revocation gate covered only the authority credential**, not history (`computeHistoryConstraints` never inspects `HistoryTrustDecision.revocation.outcome`) — decided and documented (in `history-constraints.ts`, `policy-types.ts`, and ADR 0004) that this is deliberate and safe: history can only narrow, so an unchecked-revocation history observation can at worst cause an unearned REFUSAL, never an unearned permit, so extending the gate would make the engine strictly more permissive — backwards for tighten-never-loosen. Added a regression test proving the deliberate symmetry-break. **Findings 7/8 (notes)**: fixed finding 7 (small, safe) — `validatePolicy` now rejects a `ruleId` shared across ANY two rules, not just two `action-scope` rules for the same action. Finding 8 (M3 frozen, not edited) — `engine.ts`'s non-finite-value refusal (added for finding 3) doubles as the M5-side defense of M3's unvalidated-scope-finiteness gap; documented and tested with a fabricated fixture simulating a non-finite credential scope value.
- next_action: M5 is ready for RE-VERIFY (L4). The re-VERIFY pass should independently confirm: the FINDING 1 "prove teeth" revert-and-restore result is real (temporarily reintroducing the pre-fix unguarded `authority`/`history` access should reproduce the TypeErrors); the FINDING 2 mutation count (`return candidates[0]` in `permitted-scope.ts`) is genuinely ~9/225, not 1; FINDING 4's omitted-field refusal fires for a genuinely omitted field and NOT for a field no source bounds at all (the two must stay distinguishable); and that the FINDING 6 documentation in `history-constraints.ts`/`policy-types.ts`/ADR 0004 actually justifies the asymmetry rather than merely noting it. After a clean re-VERIFY, M6 (cross-agent negotiation demo) is next per PLAN.md. `DONE.html`/`PLAN.md` remain locked and were not edited.
- model: claude-opus-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
