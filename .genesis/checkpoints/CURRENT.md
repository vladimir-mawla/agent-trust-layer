# CURRENT
- active_loop: L1 BUILD (M4 findings-remediation pass complete, awaiting re-VERIFY)
- target: M4
- iteration: 2
- last_gate: `npm run typecheck` clean (tsconfig.json + tsconfig.lib.json); `npm test` 165/165 green (145 prior + 20 new regression tests, zero regressions); `npm test -- trust` 79/79 green (M4's demo command, was 59); `npm run build` (`next build --webpack`) succeeds; test process exits cleanly with no dangling timer (full 165-test run completes in ~0.5-1s wall time, exit code 0 — see FINDING 2 below); `git diff e2720e8 -- lib/identity/ lib/credentials/ app/` empty (M1/M3/M2 untouched); `.genesis/DONE.html`/`.genesis/PLAN.md` untouched (locked); `git status --short` clean at the end.
- last_action: Fixed all defects from an independent L4 VERIFY pass that REJECTED the first M4 BUILD. **FINDING 1 (CRITICAL, fixed)**: `checkRevocation` (`lib/trust/status-list.ts`) previously verified only that a resolved status list credential was INTERNALLY self-consistent (its `issuer` claim matched whoever signed it) — never that it was issued by the credential's own real issuer. A resolver could substitute a "clean" list signed by ANY throwaway keypair and have a genuinely-revoked credential accepted, no key compromise needed. Fixed by making `expectedIssuer: Did` a REQUIRED parameter (not optional — the compiler now refuses any call site that omits it), with `trust-decision.ts` passing the credential's own M3-verified `verifiedIssuer`. New `StatusListIssuerMismatchError`. Regression test in `status-list.test.ts` ("FINDING 1" describe block) reproduces the exact swap and asserts refusal naming the mismatch; proved it has teeth by temporarily no-op'ing the check (test failed, credential accepted as `"active"`), then restoring it (test passed). **FINDING 2 (MEDIUM, fixed)**: no timeout existed around the injected resolver, so a hanging resolver hung the whole trust decision forever. Added `resolverTimeoutMs` (default `DEFAULT_RESOLVER_TIMEOUT_MS` = 5s, caller-overridable), timing out to `{ outcome: "indeterminate" }`; the timer is `clearTimeout`'d on settle and `unref()`'d as defense in depth against a dangling timer. Tested both directions (hangs → refused; slow-but-successful within window → still succeeds). **FINDING 3 (LOW-MEDIUM, fixed)**: `decodeEncodedList` (`lib/trust/bitstring.ts`) had only an accidental decompression-bomb bound (via `MAX_JWS_LENGTH`). Added explicit `MAX_DECOMPRESSED_BITSTRING_BYTES` (8MB, documented arithmetic) passed as `gunzipSync`'s `maxOutputLength`, routed through the existing `StatusListMalformedError` path (never throws raw). **FINDINGS 4-6 (LOW, fixed)**: created `lib/trust/jws-lite.test.ts` (the file `jws-lite.ts`'s comment already claimed existed) with the full algorithm-confusion battery (`none`, `HS256` HMAC'd with the public key, `RS256`, case/whitespace variants of `EdDSA`, absent/number/array `alg`, size cap, signature-before-JSON-parse ordering) — all refused; corrected `anchors.ts`'s comment, which cited a nonexistent `vouch-depth.test.ts` (the real test is in `anchors.test.ts`); corrected ADR 0003's and `status-list.ts`'s "ONLY async function ... `grep -n \"async\"` returns one match" claim, which was false (returns 4) — restated as "only function that calls the resolver directly" with a grep that actually returns one match. **M5 risk (recorded, not fixed, per instructions)**: added a warning comment on `RevocationNotChecked` in `trust-decision.ts` flagging that `not-checked` must be a deliberate policy input for M5, never silently folded into "accepted, therefore fine". Updated ADR 0003 with an "Amendment — L4 VERIFY findings" section. 20 new tests (145 → 165); `lib/identity/`, `lib/credentials/`, `app/` diffs against `e2720e8` remain empty.
- next_action: M4 is ready for re-VERIFY. The re-VERIFY pass should independently confirm: `expectedIssuer` truly cannot be omitted at any call site (grep for `checkRevocation(` and check every call supplies 3 positional args before options); the resolver-timeout default (5s) is sane for the judge-demo path and doesn't fire spuriously; the new `MAX_DECOMPRESSED_BITSTRING_BYTES` bound doesn't reject any legitimate fixture; and that no other comment in `lib/trust/` makes an unverified claim of the kind Findings 4-6 found. After that, M5 (policy engine with explanations, `lib/policy/**`) is next per PLAN.md, and must treat `RevocationNotChecked` as a required policy input, not an implicit pass — see the warning comment added to `trust-decision.ts` this pass. `DONE.html`/`PLAN.md` are locked and were not edited.
- model: claude-sonnet-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
