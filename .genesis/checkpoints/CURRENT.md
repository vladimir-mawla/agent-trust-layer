# CURRENT
- active_loop: L1 BUILD (M9, deliverables, complete on `m9-deliverables` — awaiting L4 VERIFY and user sign-off)
- target: M9
- iteration: 1
- last_gate: On `m9-deliverables`: `npm run typecheck` clean (both `tsc -p tsconfig.json` and `tsc -p tsconfig.lib.json`); `npm test` 388/388, unchanged from M8's close (this milestone is docs-only — `app/milestones.test.ts`'s drift guard still passes, unmodified); `npm test -- attacks` 76/76 across 20 files, unchanged; `npm run build` succeeds, same route table as M8 (`/`, `/api/health`, `/api/negotiate`, all `ƒ`); `npm run demo:negotiation` exits 0, all four beats as expected; `curl -sf https://agent-trust-layer-pi.vercel.app/api/health` returns `200` with `"status":"ok"` at commit `340bd8b` (the live deploy's HEAD, matching `main`); the PLAN.md M9 demo command (`git clone` the PUBLIC remote, `npm ci`, `npm run typecheck`, `npm test`) run for real against a fresh temp clone of `main` — passes, 388/388, and (since `main` predates this branch) the identical sequence re-run against a fresh temp clone of THIS branch's own tip — also passes, 388/388, confirming the M9 doc commits don't regress anything; `git diff main -- lib/ tests/ app/` EMPTY (this milestone touched only `README.md`, `docs/**`, and this checkpoint); `git status --short` clean after each commit; `git branch --show-current` → `m9-deliverables`.
- last_action: Built M9's four required deliverables from two user-provided drafts (architecture, thesis), fact-checking every claim in both against real tests before committing rather than trusting the drafts as-is — this milestone's own standard, since three earlier milestones on this project were caught with prose the code didn't back up. ARCHITECTURE (`docs/ARCHITECTURE.md`): kept the four-layer diagram and its "refuses" lines, all individually checked against `tests/attacks/named/*`, `lib/identity/challenge.test.ts`, `lib/credentials/jws.test.ts`/`verify.test.ts`, and `lib/policy/engine.test.ts`/`history-constraints.test.ts` — all held except two the draft got wrong: the IDENTITY layer's "refuses a replayed challenge" line credited the wrong layer (verifyPossession's own "REPLAY LIMITATION" comment says it does NOT deduplicate nonces; that's `lib/negotiation/supplier.ts`'s job), and "exactly one function in the entire library is async" is false today (`grep -n "async" lib/` returns seven matches, not one — ADR 0003 itself already flags this exact miscount) where the true, checkable claim is that exactly one function (`checkRevocation`) ever calls the injected resolver. Both rewritten to state what's actually true. THESIS (`docs/THESIS.md`): used near-verbatim (287 words excluding the H1, 296 including it — under 300 either way), since its claims are forward-looking argument rather than assertions about this codebase to verify. NOTES (`docs/NOTES.md`): AI tooling, the four ADRs' decisions summarized with why, what's deliberately out of scope (no signed range, no non-numeric scope bounds, no PoP rate-limiting, did:key only, no shared nonce store, depth-1 vouching only), and the genesis reject record — checked the "six of nine milestones rejected" framing against the actual commit/ADR history rather than repeating it uncritically: found explicit REJECT-and-reverify cycles for exactly four milestones (M3, M4, M5, M6 — M6 twice), each cited with a real defect (M4's status-list-issuer-swap bypass, M5's omitted-bounded-scope-field bypass behind 31 passing tests), plus M1 and M2 each having one verifier-found finding fixed pre-merge without formal REJECT language — reconciling with PR #5's own "six of eight" running count without asserting a flatter, less accurate number. WALKTHROUGH (`docs/WALKTHROUGH.md`): a beat-by-beat 90-second Loom script built by actually reading the live page's current copy and section order (claim → Beat 1 accepted → Beat 2 over-scope refusal naming the field/numbers → Beat 3 spoofed-identity climax, ~40 of the 90 seconds → closing on why there's no score), with two optional beats for a recording running short. README: full rewrite for the finished nine-milestone state (was M2-era, claiming VCs "planned for M3" and two-of-nine done) — live URL and real curl response first, the claim up front, links to all four new docs, a verified 388-test / 76-attack-test / six-named-regression table, quickstart re-run against this tree, trimmed dev notes (webpack/extensionAlias, npm ci-vs-install with the exact dropped package names read from `.github/workflows/ci.yml`), and a new, unsoftened "Honest limits" section covering all seven limits named for this milestone. Fixed one dead link carried forward from the original README (`w3c-ccg.github.io/did-method-key/` 404s; replaced with `w3c-ccg.github.io/did-key-spec/`, confirmed 200) and checked every other link (internal file paths and external W3C spec URLs) resolves. Did NOT touch `lib/**`, `tests/**`, `app/**`, `.genesis/DONE.html`, or `.genesis/PLAN.md` — and did not flip M9 to "done" in either `DONE.html` or `app/milestones.ts`, per this milestone's own instruction that the user makes that call after their own verification.
- next_action: L4 VERIFY on `m9-deliverables`, then user sign-off. If APPROVED, mark M9 done in `.genesis/DONE.html` and `app/milestones.ts` (not done by this pass) — this is the final milestone.
- model: claude-sonnet-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
- iteration_M8_history: M8 (the demo UI) completed on `m8-demo-ui`, merged via PR #5, with two polish fixes landing after the independent verification pass (Beat 3 signposted as the centerpiece; a colour-token fix so the DID-match highlight no longer reused the "permit" colour for what is actually the attack) — see this file's prior revision (preserved in `git log -p` on this file) for the full M8 build record.
