# CURRENT
- active_loop: L1 BUILD (M6 SECOND fix round complete on `m6-negotiation` — awaiting re-VERIFY)
- target: M6
- iteration: 4
- last_gate: On `m6-negotiation`: `npm run typecheck` clean; `npm test` 298/298 (was 288 before this round, +10 new tests, zero weakened/deleted); `npm run demo:negotiation` prints all four beats and exits 0; `npm run build` succeeds; `lib/identity/`, `lib/credentials/`, `lib/trust/`, `app/` diff against `main` empty; `.genesis/DONE.html` diff against `main` empty; `git status --short` clean after committing. Re-ran mutation testing on the two 0-failure findings from this SAME round plus the previously-fixed nonce-consumption-ordering mutation: gutting `sanitizeScope` to a reference passthrough now fails 1 test (was 0); removing the vouch cap `.slice` now fails 1 test (was 0); restoring the old "consume-before-verify" order now fails 1 test. `git diff main -- .genesis/PLAN.md` is still non-empty for the same pre-existing, pre-this-round reason recorded in iteration 3 below — nothing in EITHER fix round touched `PLAN.md`.
- last_action: Fixed all four findings from the SECOND, independent L4 VERIFY REJECT of M6 — every one in code the FIRST fix round itself introduced (the nonce cache, the vouch cap). (A, CRITICAL) `Supplier.#pruneExpiredNonces` evicted a consumed-nonce entry at `expiresAt <= now`, but M1's frozen `verifyPossession` only refuses at `now > expiresAt` — a one-millisecond disagreement at the exact instant `now === expiresAt` let pruning forget a nonce proof-of-possession still considered live, reopening the precise replay window FIX 3 (round one) existed to close. Fixed by making the prune predicate the exact logical complement of `verifyPossession`'s check (`expiresAt < now`, never restated as an independent constant — the comment now says explicitly that a future edit to either side must re-derive the other). Proven with fake timers at `expiresAt-1`/`expiresAt`/`expiresAt+1`; reverting to `<=` makes the exact-instant test fail (permits the replay), restoring makes it pass. (B) Consumption used to mark a nonce spent the INSTANT a presentation cited it, before its signature was checked — so anyone who merely knew/intercepted a challenge nonce (public the moment it's on the wire) could burn the legitimate holder's one shot with a garbage signature, without holding any private key: a pure DoS. Fixed by reordering to check-already-consumed -> verify -> consume-only-on-success, still entirely before credential/policy evaluation and still fully synchronous relative to every check above it (no `await` yet), so the existing "two racing calls, exactly one permitted" concurrency property is unaffected — proven by both a dedicated race test and by re-confirming the pre-existing one still passes. TRADE-OFF recorded: the guarantee is now "a challenge may be *successfully* answered at most once", not "presented at most once" — a failing signature no longer costs anyone anything, but that also means an attacker gets unlimited free (and cheap) retries against a live challenge before the real holder answers it or it expires; no rate-limiting was added, by design, since that's a deployment concern orthogonal to this protocol gate. (C) Two mutations broke 0 of 288 tests: gutting `sanitizeScope` to pass its input through BY REFERENCE, and removing the vouch cap's `.slice`. Both had zero coverage for the same underlying reason — a downstream guard (`lib/policy`'s own `safe-scope-read.ts` guard for the first; nothing at all for the second, since no test ever exercised >2 vouches) made the outcome-level behavior identical with or without the fix, so testing THROUGH the pipeline could never see the difference. Fixed by (1) exporting `sanitizeScope` (test-only reason documented inline) and unit-testing its return value's REFERENCE identity and post-call mutation-isolation directly, and (2) a new integration test sized off the (now-exported) `MAX_VOUCHES_PER_PRESENTATION` constant that puts one real, verifiable vouch at position 17 (dropped, refused) vs. within the first 16 (kept, permitted) — both proven to fail against their respective mutants, then restored. (D) `supplier.ts`'s module comment now carries an explicit deployment warning: single-use is tracked per-`Supplier`-INSTANCE, in memory; a stateless server that builds a fresh `Supplier` per request silently defeats it entirely (every request starts with an empty nonce cache), and a real multi-process/per-request deployment needs the same nonce bookkeeping moved to shared storage (Redis, a DB table, ...) with the same verify-before-consume ordering and TTL-based eviction — comment only, no storage built.
- next_action: Re-run L4 VERIFY on `m6-negotiation` / PR #2. If APPROVED, merge and proceed to M7 (the attack suite), reusing `lib/negotiation`'s `Supplier`/`KeyHolder`/`scenario.ts` as fixtures.
- model: claude-opus-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
- iteration_3_history: active_loop L1 BUILD (M6 first fix round complete on `m6-negotiation`); last_gate: `npm run typecheck` clean, `npm test` 288/288 (was 262 after the `main` merge), demo exits 0, build succeeds, frozen-dir diffs against `main` empty, `.genesis/DONE.html` diff against `main` empty, `git status --short` clean after committing — NOTE `git diff main -- .genesis/PLAN.md` was (and remains) non-empty for a one-line diff (`lib/agents/**` -> `lib/negotiation/**`) from commit `3f753b8`, already on `m6-negotiation` before either fix round, an earlier legitimate correction of M6's own freeze-boundary path; last_action: fixed the FIRST L4 VERIFY REJECT's five findings (hostile-scope-accessor hardening at both `lib/policy/safe-scope-read.ts` and `lib/negotiation/supplier.ts`'s `sanitizeScope`; corrected two misleading `.resolves`-only test assertions into pinned outcomes; added challenge single-use via `#consumedChallengeNonces`; wired `PresentedCredentials.vouches` through to `evaluateAuthorityCredentialTrust` behind a 16-entry cap; hardened `decision-guards.ts` against a throwing property read in `authority`/`history`).
