# CURRENT
- active_loop: L1 BUILD (M8, the demo UI, complete on `m8-demo-ui` — awaiting L4 VERIFY)
- target: M8
- iteration: 1
- last_gate: On `m8-demo-ui`: `npm run typecheck` clean (both `tsc -p tsconfig.json` and `tsc -p tsconfig.lib.json`); `npm test` 383/383 (was 374 at M7's close, +9 new in `app/lib/negotiation-service.test.ts`, zero weakened/deleted; `app/milestones.test.ts` still passes unchanged); `npm run build` succeeds, route table shows `/`, `/api/health`, and `/api/negotiate` all `ƒ` (Dynamic) — deliberately, since `getFixedFourBeats`/`runCustomNegotiation` generate fresh Ed25519 keys per call and a statically-prerendered `/` would freeze one run's cast forever; `npm run demo:negotiation` still exits 0, unmodified; `curl -sf localhost:3000/api/health` still 200; `curl -s localhost:3000/` greps to exactly 4 `data-testid="decision"` matches (PLAN.md's own M8 demo command) and contains all four beat titles server-rendered with no client JS required; `curl -s localhost:3000/api/negotiate` (GET) and four POST calls (one per attack kind) all return real structured `NegotiationDecision`/`Explanation` JSON with the right `refusalKind` per kind (`over-scope`, `negative-scope-value`, `spoof-did` → `stage:"proof-of-possession"`, `untrusted-issuer`, `revoked`); grepping the homepage HTML and every one of those API responses, case-insensitive, for "privatekey"/"secretkey" and for any 64-hex-char token (the shape of either a raw private or public Ed25519 key) returns zero matches in all cases; `git diff main -- lib/ tests/ .genesis/DONE.html .genesis/PLAN.md` EMPTY; `git status --short` clean after committing; `git branch --show-current` → `m8-demo-ui`. Verified at both 390px and 400px viewport widths (and, defensively, down to 280px) with zero horizontal `document.documentElement.scrollWidth` overflow, in both light and dark `prefers-color-scheme`, using the Browser tool's live JS/DOM inspection (not just a visual screenshot) to find and fix three real CSS grid/flexbox "blowout" bugs (an implicit `1fr`/flex min-content width, and a `<table>`+flex `display` override quirk) that a screenshot alone would have made easy to miss.
- last_action: Built the M8 demo UI entirely under `app/**` (the milestone's own freeze boundary), touching neither `lib/**` nor `tests/**`. ARCHITECTURE: negotiation runs server-side only (`app/api/negotiate/route.ts`, `runtime="nodejs"`, `dynamic="force-dynamic"`) because `lib/trust/bitstring.ts` imports `node:zlib` for the W3C Bitstring Status List, which has no browser build — `app/lib/negotiation-service.ts` composes ONLY `lib/negotiation`'s + `lib/trust`'s own public barrel exports (`KeyHolder`, `Supplier`, `TrustAnchorSet`, `buildFourBeatFixture`/`runFourBeatScenario`) to (a) reuse M6's real four-beat scenario unchanged and (b) run one extra, freshly-built interactive negotiation per request (a clean cast + a status list with one deliberately-revoked index, so "present as: revoked credential" is a real `checkRevocation` refusal, not a fake one). `app/lib/decision-view.ts` is a second, TYPE-ONLY-import module (erased at compile time, so it pulls in no `node:zlib` at all) shared by both the server-rendered fixed beats and the client-rendered interactive result, so the two can never render a decision two different ways. PAGE: one sentence at the top, then the cast + policy + all four beats fetched and server-rendered directly in the `page.tsx` Server Component (no button, no client JS needed — `curl` alone shows them), then the one interactive control (`InteractivePanel`, "use client"), then an OPTIONAL browser-side identity panel (`IdentityPanel`, "use client", imports `lib/identity` directly — zero `node:` imports, confirmed to bundle and run client-side via `npm run build` and a live click-through), then the existing milestone ledger and links (untouched in substance, `app/milestones.test.ts` still passes). BEAT 3: `BeatCard` receives the claimed DID and the real buyer DID as two explicit props and computes their equality itself (`display.claimedDid === realOwnerOfClaimedDid`) — never a hardcoded "they match" string — then renders both truncated DIDs side by side in a highlighted `did-compare` box with an explicit "character-for-character identical" callout when they do match, plus the stage label ("proof of possession — refused before any credential was examined"). BEAT 4: an explicit `annotation` prop states "the attacker's own signature on this credential is genuinely valid... refused because the attacker is not a trust anchor" — supplementary prose, never a substitute for the `gate:issuer-trust`/`self-issued` fields rendered above it. INTERACTION: one control (amount number input + three presets including `-500`, plus a 4-way "present as" radio: honest / spoof-did / forge-credential / revoked-credential) POSTs to `/api/negotiate`, which runs the real `Supplier.evaluatePresentation` and returns a real decision — verified live via the Browser tool for all five (four attacks + honest) request shapes. EXPLANATION RENDERING: `toDisplayDecision` (`decision-view.ts`) derives every string/chip solely from `NegotiationDecision`/`Explanation`'s structured `rule`/`field`/`narrative`/`refusalKind` — no per-beat template. LEAK CHECK: no `KeyHolder` instance (only `.did`) ever crosses a DTO boundary, and `#privateKey` is a true JS private class field invisible to `JSON.stringify` regardless; confirmed by grep (see last_gate) and by a dedicated `assertNoRawKeyLooking` check in `app/lib/negotiation-service.test.ts`.
- next_action: L4 VERIFY on `m8-demo-ui`. If APPROVED, merge and proceed to M9 (deliverables).
- model: claude-opus-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
- iteration_M7_history: see this file's prior revision (M7, the attack suite, two iterations on `m7-attack-suite`: build round found and flagged a real `lib/policy` defect — negative requested scope values were permitted — then a fix round closed it with a new `"negative-scope-value"` `RefusalKind`, an ADR 0004 amendment, and 9 new tests). `git log` on `main`/`m7-attack-suite` carries the full history predating this checkpoint.
