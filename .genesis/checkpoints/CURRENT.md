# CURRENT
- active_loop: L1 BUILD (M7, the attack suite plus its own negative-scope-value fix, complete on `m7-attack-suite` — awaiting L4 VERIFY)
- target: M7
- iteration: 2
- last_gate: On `m7-attack-suite`: `npm run typecheck` clean (both `tsc -p tsconfig.json` and `tsc -p tsconfig.lib.json`); `npm test` 374/374 (was 365 at the end of iteration 1, +9 new in the fix round, zero weakened/deleted); `npm test -- attacks` 76/76 (20 files); `npm run demo:negotiation` prints all four beats and exits 0, unaffected by the fix; `npm run build` succeeds; `git diff main -- lib/identity/ lib/credentials/ lib/trust/ lib/negotiation/ app/` EMPTY (only `lib/policy/**`, one test file, and `.genesis/decisions/0004-...md` touched this round); `git status --short` clean after committing; teeth proven by reverting `lib/policy/engine.ts`'s fix and re-running the corrected/added tests in `tests/attacks/exploratory/04-numeric-scope-edge-cases.test.ts` (7 of them fail by PERMITTING the negative request, as expected), then restoring the fix (all 13 pass again).
- last_action (iteration 1, build round): Built the M7 attack suite under `tests/attacks/**` (PLAN.md's own path), reusing `lib/negotiation`'s `Supplier`/`KeyHolder`/`buildFourBeatFixture` as fixtures throughout rather than re-deriving a harness. PART 1 (10 named attacks, `tests/attacks/named/`): forged signature, tampered claim, stolen-credential-different-subject, captured-presentation-replayed, expired, revoked, self-issued authority (explicitly asserted NOT `credential-verification-failed` — the attacker's signature is genuinely valid, per the brief's own callout), DID impersonation (proved structurally with a resolver spy that must never be called), over-scope, confused deputy (wrong-action). Each asserts the NAMED mechanism/step/gate and explicitly excludes the neighboring wrong gates. PART 2 (6 real historical regressions, `tests/attacks/regressions/`, each dated and cited to its own ADR/L4-review finding): #11 the M3 non-JSON-payload crash (now returns a structured "parse" failure from both verify functions, never throws); #12 the M4 status-list-issuer-swap bypass (ADR 0003's own "Amendment" finding 1); #13 the M5 omitted-bounded-scope-field bypass (ADR 0004 FINDING 4, the literal `scope: {}` shape); #14 the M6 throwing-scope-accessor crash (FIX 1, first L4 M6 review) — extended BEYOND the existing `engine.test.ts`/`supplier.test.ts` battery with a `has`-trap Proxy, a getter that throws only on its SECOND read (proving the engine reads a field at most once), a self-mutating enumeration hazard (proving `Reflect.ownKeys`'s snapshot-before-iterate ordering defeats it), and Symbol/BigInt-returning getters (proving neither reaches an unguarded `JSON.stringify`); #15 the one-millisecond nonce-prune boundary (FIX A, second L4 M6 review), pinned with fake timers at `expiresAt-1`/`expiresAt`/`expiresAt+1`; #16 the consume-before-verify DoS (FIX B, second L4 M6 review), proving BOTH that the legitimate holder is no longer burned by a garbage-signed impostor attempt AND that single-use still holds for a real presentation. PART 3 (exploratory, `tests/attacks/exploratory/`): tried cross-supplier credential reuse (held — bounded by each supplier's own anchors/policy, no audience claim exists so this is by design, not a gap), mixed valid+forged history attestations in one presentation (held — evaluated independently, forged entry silently dropped), a history attestation crafted to widen (held — Math.min always wins even against an absurd policy-authored `narrowedMax`), a 3-node vouch cycle (held — `VOUCH_DEPTH_LIMIT=1` means no chain-walk exists to loop, refused fast), a status-list self-revocation idea (does not apply — no `credentialStatus` field on `BitstringStatusListCredential` itself; tested the closest real variant, feeding an unrelated real credential to the resolver as type-confusion, correctly rejected as malformed), unicode/homoglyph action-name and DID tricks (held — exact string equality, and base58btc's alphabet excludes the ambiguous characters that make homoglyphs possible), action-name case/whitespace normalization mismatches (held — no normalization anywhere, fails closed as `no-matching-rule`), and numeric edge cases (`Number.MAX_SAFE_INTEGER`, `1e308`, `-0` all held) — ONE genuine, NEW finding surfaced and is flagged, NOT fixed: `lib/policy`'s scope-bound arithmetic enforces only an upper ceiling (`Math.min` + `requested > bound`), never a lower bound, so a NEGATIVE requested scope value (e.g. `amount: -500000`) is currently PERMITTED. Recorded in `tests/attacks/exploratory/04-numeric-scope-edge-cases.test.ts`'s own large comment and in the M7 task report's ANY_NEW_DEFECT section — needs its own review cycle, per the M7 brief's own instruction not to fix frozen `lib/` code from inside this milestone. A separate, ad hoc (uncommitted) timing measurement also confirmed the fail-fast gate ordering does leak SOME timing signal (a malformed-shape refusal is measurably faster than a full credential+revocation+policy evaluation) — an inherent, acknowledged property of an early-exit design already documented throughout `lib/`'s own comments, not something this milestone treats as a defect to fix. Resolved the tsconfig gap PLAN.md's own path created (`tests/attacks/**` was covered by NEITHER `tsconfig.json` (app-only) NOR the old `tsconfig.lib.json` (lib-only)) by widening `tsconfig.lib.json`'s own `include` array to also reach the `tests` tree — not `tsconfig.json` (wrong resolution mode: NodeNext vs. bundler) and not `package.json`'s script (no change needed, `tsconfig.lib.json` is not in `context-graph.json`'s freeze boundary; `lib/**`, `app/**`, `package.json`, `tsconfig.json` are, `tsconfig.lib.json` specifically is not).
- last_action (iteration 2, fix round): M7's own exploratory suite found a real defect (see iteration 1's `last_action` below) — `lib/policy`'s scope arithmetic enforced only an upper ceiling, never a lower bound, so a negative requested scope value (e.g. `amount: -500000` against a credential granting `maxAmount: 500`) was PERMITTED. Reviewed and fixed in `lib/policy/engine.ts` (the one frozen-adjacent module this milestone was given write access to): a requested numeric scope value must now be `>= 0`, checked immediately after the existing `Number.isFinite` guard and refused with a NEW, distinct `RefusalKind` — `"negative-scope-value"` (`lib/policy/explanation.ts`, now ten kinds, not nine) — never folded into `"over-scope"`, since "over scope" would misdescribe a negative request as "too much" rather than "the wrong sign". `0` and `-0` remain deliberately PERMITTED (harmless zero-usage, and the same real-world magnitude). Documented as a deliberately NARROW fix, not a general lower-bound/signed-range policy language, in both `engine.ts`'s own comment and a new ADR 0004 amendment ("Amendment — M7 exploratory attack suite finding"). `tests/attacks/exploratory/04-numeric-scope-edge-cases.test.ts`'s own `[FINDING — flagged, not fixed]` test (which had asserted the bug's permissive behavior on purpose, per the M7 brief) is corrected to assert the refusal and its new `refusalKind`, with the original finding's provenance kept in the comment rather than deleted; 9 new tests added alongside it covering zero-permitted, `-0` (already covered, decision reaffirmed), `Number.MIN_SAFE_INTEGER`, `-1e308`, `-Infinity`/`-NaN` (both non-finite, so refused as the PRE-EXISTING `"over-scope"` gate, not the new kind — decided and tested explicitly), a field bounded only by the credential's own scope, a field bounded only by a triggered history-narrow rule, a mixed valid+negative request, and a negative value arriving via a non-throwing getter. Searched the full test suite for any OTHER test asserting a negative scope value as permitted through the policy engine — found none; the one test that did (the exploratory finding test itself) is the one corrected here. Teeth proven: reverting the `engine.ts` fix makes exactly the 7 negative-value assertions fail by permitting (the pre-existing `-Infinity`/`-NaN` tests still pass, as expected, since those go through the untouched `Number.isFinite` gate); restoring the fix makes all 13 pass again.
- next_action: L4 VERIFY on `m7-attack-suite`. If APPROVED, merge and proceed to M8 (the demo UI).
- model: claude-sonnet-5
- tokens_used: ~unspecified (not tracked by this harness)
- tokens_budget: 150000
- skills_loaded: []
- iteration_M6_history: M6 (cross-agent negotiation) completed across two independent L4-rejected fix rounds on `m6-negotiation`, merged to `main` (`c9b8a4a`), then M5+M6 marked done in `DONE.html` (`2d6f187`). Final M6 state: `npm test` 298/298, typecheck clean, demo exits 0, build succeeds, all frozen-dir diffs against `main` empty. Fix round 2 closed: (A, CRITICAL) a one-millisecond nonce-prune/expiry comparator disagreement; (B) consuming a challenge nonce before verifying the signature over it (a DoS); (C) two zero-coverage mutations in `sanitizeScope`/the vouch cap; (D) documented the per-instance-only single-use guarantee. See `git log` on `main` for the full commit history predating this checkpoint.
